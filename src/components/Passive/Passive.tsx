import '../../styles/components/passive.scss'

export const Passive: React.FunctionComponent = () => {
  
  return(
    <section className="passive">
      <h1>Passive(in progress)</h1>
    </section>
  )
}